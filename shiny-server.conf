# Shiny Server config – puerto dinamico para Render
run_as shiny;

server {
  # Render inyecta $PORT; fallback a 3838 en local
  listen ${PORT:-3838};

  location / {
    site_dir /srv/shiny-server;
    log_dir  /var/log/shiny-server;
    directory_index on;
  }
}
